arab
9z8j.
Lunar Client

arab

 — 2/15/2026 6:47 PM
Image
Image
arab

 — 2/15/2026 7:06 PM
import discord
from discord.ext import commands
from PIL import Image, ImageDraw, ImageFont, ImageFilter
import datetime
import aiohttp
import io

message.txt
8 KB
f3pv — 2/15/2026 7:55 PM
https://discord.gg/rVfWts5M
Arab's Studio
Arab's Studio
15 Online
43 Members
Est. Feb 2026

Go to Server
f3pv — 2/15/2026 8:27 PM
Moderation symbols:
:wn:  Warning
:mt:   Mute (duration may vary)
:tb:   Temporary Ban (duration may vary)
:bn:   Permanent Ban

By participating in this server, you agree to follow Discord's Terms of Service and Community Guidelines:

:ds: Discord's ToS: https://discord.com/terms
:ds: Discord's Community Guidelines: https://discord.com/guidelines

:h0::hs: Do not ping Hazem
Pinging Hazem, any of his alts, or replying to an old message of his with reply ping on is not allowed. :tb:

:h1::hs: Be respectful & use common sense
Show respect to other users of the server and use common sense. If something isn't listed here, it doesn't grant you permission to do it. :mt: / :tb:

:h2::hs: No NSFW or disturbing imagery
Gore, NSFW and other suggestive imagery is not tolerated, no matter which channel it is sent in. If it's something people might not want to see, don't post it. :bn:

:h3::hs: No banned usernames.
Your username cannot be blank or use an invisible unicode character. This makes it harder for our moderators to right click on your name. You also cannot make your name "Hazem". If you're caught with one, you will be moderated. :wn: / :tb: / :bn:
:h4::hs: No advertising.
Unwanted advertisements posted in text channels or DMs are not tolerated, unless you're given permission by the user you're sending it to. This includes Discord server invites, YouTube videos, Twitch streams, tweets and other content. This also applies to content you're promoting that isn't yours. This rule doesn't apply in channels where advertising is allowed, such as ⁠pd-content. Advertising commissions is now allowed in ⁠creations. :wn: / :mt: / :tb:

:h5::hs: No spamming.
Self explanatory; spamming, text walls and posting multiple large images at once is not tolerated in this server. :mt:

:h6::hs: No begging.
Don't ask for donations here, only do it in the PLS DONATE game. Doing so here will get you banned. Furthermore, witch-hunting rich players or actively scouting for rich servers in this Discord server will result in a ban. :tb:

:h7::hs: No excessive swearing/profane language.
Swearing is allowed, but not excessively or in a disrespectful manner, including statements that moderators may deem inappropriate. :mt: / :tb: / :bn:

:h8::hs: Do not abuse the ticket system.
The ticket system should only be used for serious concerns; only use the tickets system to report users or request support. :tb: / :bn:

:h9::hs: No mass pinging users.
Mass mentioning random users or staff is not allowed. :mt: / :tb: / :bn:
:h1::h0::hs: No offensive remarks.
Using slurs (including ones you can reclaim), offensive sayings or just being disrespectful to a user in general is not tolerated, even as a joke. :tb: / :bn:

:h1::h1::hs: No impersonation.
Impersonation of others is prohibited, even as a joke or if you're given permission to. This includes purposefully using the same username, profile picture or server nickname, even if they aren't in the server. :wn: / :tb: / :bn:

:h1::h2::hs: No epileptic gifs.
Intentionally using GIFs that are meant to provoke serious reactions from people with epilepsy is prohibited. This includes GIFs and emoji with rapidly flashing colors or anything else that could provoke an epileptic reaction. :mt: / :tb: / :bn: 

:h1::h3::hs: No sharing personal information
Threatening to share / sharing personal information of any members of the server including yourself is strictly forbidden. :bn:

:h1::h4::hs: Do not bring up controversial topics.
Refrain from discussing controversial topics such as religion and politics in this server. :mt: / :tb: / :bn:

:h1::h5::hs: No bot XP farming
Farming XP for levels manually or through the use of third-party software is not allowed as it is unfair for everyone. :mt: / :tb:

:h1::h6::hs: This is an english server.
The primary language of this server is English. While you are allowed to speak in other languages briefly (for demonstration or joking purposes), do not excessively hold conversations in foreign languages. :mt: / :tb:
f3pv — 2/16/2026 12:43 AM
https://discord.gg/arabsstudio
Arab's Studio
Arab's Studio
18 Online
51 Members
Est. Feb 2026

Go to Server
f3pv — 2/16/2026 12:52 PM
#1683ff
arab

 — 2/16/2026 10:01 PM
Image
arab

 — 2/17/2026 12:25 PM
Image
arab

 — 2/17/2026 5:33 PM
https://discord.gg/dxsk
Dusk's Studio
Dusk's Studio
1,150 Online
12,645 Members
Est. May 2025
Hi, i'm dusk
Roblox
Minecraft
💸
PDM
🎁
Daily Giveaways
🔥
Active
+ 2 more

Go to Server
f3pv — 2/17/2026 5:52 PM
https://discord.gg/dxsk
Dusk's Studio
Dusk's Studio
1,150 Online
12,645 Members
Est. May 2025
Hi, i'm dusk
Roblox
Minecraft
💸
PDM
🎁
Daily Giveaways
🔥
Active
+ 2 more

Go to Server
https://discord.gg/dxsk
Dusk's Studio
Dusk's Studio
1,150 Online
12,645 Members
Est. May 2025
Hi, i'm dusk
Roblox
Minecraft
💸
PDM
🎁
Daily Giveaways
🔥
Active
+ 2 more

Go to Server
https://discord.gg/dxsk
Dusk's Studio
Dusk's Studio
1,150 Online
12,645 Members
Est. May 2025
Hi, i'm dusk
Roblox
Minecraft
💸
PDM
🎁
Daily Giveaways
🔥
Active
+ 2 more

Go to Server
https://discord.gg/dxsk
Dusk's Studio
Dusk's Studio
1,150 Online
12,645 Members
Est. May 2025
Hi, i'm dusk
Roblox
Minecraft
💸
PDM
🎁
Daily Giveaways
🔥
Active
+ 2 more

Go to Server
https://discord.gg/dxsk
Dusk's Studio
Dusk's Studio
1,150 Online
12,645 Members
Est. May 2025
Hi, i'm dusk
Roblox
Minecraft
💸
PDM
🎁
Daily Giveaways
🔥
Active
+ 2 more

Go to Server
https://discord.gg/dxsk
Dusk's Studio
Dusk's Studio
1,150 Online
12,645 Members
Est. May 2025
Hi, i'm dusk
Roblox
Minecraft
💸
PDM
🎁
Daily Giveaways
🔥
Active
+ 2 more

Go to Server
https://discord.gg/dxsk
Dusk's Studio
Dusk's Studio
1,150 Online
12,645 Members
Est. May 2025
Hi, i'm dusk
Roblox
Minecraft
💸
PDM
🎁
Daily Giveaways
🔥
Active
+ 2 more

Go to Server
f3pv — Yesterday at 9:27 PM
https://discord.com/invite/zYdW7hRxyB
arab

 — Yesterday at 9:36 PM
https://discord.com/invite/zYdW7hRxyB
https://discord.com/invite/zYdW7hRxyB
https://discord.com/invite/zYdW7hRxyB
https://discord.com/invite/zYdW7hRxyB
https://discord.com/invite/zYdW7hRxyB
https://discord.com/invite/zYdW7hRxyB
https://discord.com/invite/zYdW7hRxyB
https://discord.com/invite/zYdW7hRxyB
f3pv — 12:51 AM
import discord
from discord.ext import commands
from PIL import Image, ImageDraw, ImageFont, ImageFilter
import datetime
import aiohttp
import io
import os

TOKEN = os.getenv("TOKEN")
WELCOME_CHANNEL_ID = 1472224372382109905

intents = discord.Intents.default()
intents.members = True
intents.message_content = True

bot = commands.Bot(command_prefix="!", intents=intents)


@bot.event
async def on_ready():
    print(f"Logged in as {bot.user}")


async def create_welcome_gif(member):
    width, height = 1000, 400
    frames = []

    font_title = ImageFont.truetype("Montserrat-Bold.ttf", 70)
    font_user = ImageFont.truetype("Montserrat-Regular.ttf", 40)
    font_small = ImageFont.truetype("Montserrat-Regular.ttf", 28)
    font_logo = ImageFont.truetype("Montserrat-Bold.ttf", 110)
    font_link = ImageFont.truetype("Montserrat-Regular.ttf", 24)

    sequences = [
        ["W","WE","WEL","WELC","WELCO","WELCOM","WELCOME"],
        ["W","WI","WIL","WILL","WILLK","WILLKO","WILLKOM","WILLKOMM","WILLKOMME","WILLKOMMEN"],
        ["B","BE","BEN","BENV","BENVE","BENVEN","BENVENU","BENVENUT","BENVENUTO"],
    ]

    username = member.display_name
    member_count = f"Member #{member.guild.member_count}"
    join_time = datetime.datetime.now(datetime.timezone.utc).strftime("%H:%M UTC")

    # DARK DIAGONAL BACKGROUND
    base_bg = Image.new("RGB", (width, height))
    bg_draw = ImageDraw.Draw(base_bg)

    for y in range(height):
        for x in range(width):
            ratio = (x + y) / (width + height)
            r = int(55 - ratio * 30)
            g = 0
            b = int(105 - ratio * 50)
            bg_draw.point((x, y), fill=(r, g, b))

    base_bg = base_bg.convert("RGBA")

    # AVATAR
    async with aiohttp.ClientSession() as session:
        async with session.get(member.display_avatar.url) as resp:
            avatar_bytes = await resp.read()

    avatar = Image.open(io.BytesIO(avatar_bytes)).convert("RGBA")
    avatar = avatar.resize((110, 110))

    mask = Image.new("L", (110, 110), 0)
    ImageDraw.Draw(mask).ellipse((0, 0, 110, 110), fill=255)
    avatar.putalpha(mask)

    spacing = 60
    typing_speed = 6

    cycle_lengths = [len(seq) * typing_speed for seq in sequences]
    total_cycle = sum(cycle_lengths)
   
    total_frames = total_cycle + 30

    for frame in range(total_frames):
        img = base_bg.copy()
        draw = ImageDraw.Draw(img)

        # XO pattern
        pattern_layer = Image.new("RGBA", (width * 2, height), (0, 0, 0, 0))
        p_draw = ImageDraw.Draw(pattern_layer)

        for y in range(0, height, spacing):
            for x in range(0, width * 2, spacing):
                p_draw.text((x, y), "X", font=font_small, fill=(255, 255, 255, 50))
                p_draw.text((x + 25, y + 25), "O", font=font_small, fill=(255, 255, 255, 50))

        offset = (frame * 4) % spacing
        cropped_pattern = pattern_layer.crop((offset, 0, offset + width, height))
        img = Image.alpha_composite(img, cropped_pattern)

        draw = ImageDraw.Draw(img)

        # Language typing logic
        cycle_frame = frame % total_cycle
        cumulative = 0

... (123 lines left)

message.txt
8 KB
https://discord.gg/arabsstudio
Arab's Studio
Arab's Studio
18 Online
51 Members
Est. Feb 2026

Go to Server
﻿
f3pv
f3pv
import discord
from discord.ext import commands
from PIL import Image, ImageDraw, ImageFont, ImageFilter
import datetime
import aiohttp
import io
import os

TOKEN = os.getenv("TOKEN")
WELCOME_CHANNEL_ID = 1472224372382109905

intents = discord.Intents.default()
intents.members = True
intents.message_content = True

bot = commands.Bot(command_prefix="!", intents=intents)


@bot.event
async def on_ready():
    print(f"Logged in as {bot.user}")


async def create_welcome_gif(member):
    width, height = 1000, 400
    frames = []

    font_title = ImageFont.truetype("Montserrat-Bold.ttf", 70)
    font_user = ImageFont.truetype("Montserrat-Regular.ttf", 40)
    font_small = ImageFont.truetype("Montserrat-Regular.ttf", 28)
    font_logo = ImageFont.truetype("Montserrat-Bold.ttf", 110)
    font_link = ImageFont.truetype("Montserrat-Regular.ttf", 24)

    sequences = [
        ["W","WE","WEL","WELC","WELCO","WELCOM","WELCOME"],
        ["W","WI","WIL","WILL","WILLK","WILLKO","WILLKOM","WILLKOMM","WILLKOMME","WILLKOMMEN"],
        ["B","BE","BEN","BENV","BENVE","BENVEN","BENVENU","BENVENUT","BENVENUTO"],
    ]

    username = member.display_name
    member_count = f"Member #{member.guild.member_count}"
    join_time = datetime.datetime.now(datetime.timezone.utc).strftime("%H:%M UTC")

    # DARK DIAGONAL BACKGROUND
    base_bg = Image.new("RGB", (width, height))
    bg_draw = ImageDraw.Draw(base_bg)

    for y in range(height):
        for x in range(width):
            ratio = (x + y) / (width + height)
            r = int(55 - ratio * 30)
            g = 0
            b = int(105 - ratio * 50)
            bg_draw.point((x, y), fill=(r, g, b))

    base_bg = base_bg.convert("RGBA")

    # AVATAR
    async with aiohttp.ClientSession() as session:
        async with session.get(member.display_avatar.url) as resp:
            avatar_bytes = await resp.read()

    avatar = Image.open(io.BytesIO(avatar_bytes)).convert("RGBA")
    avatar = avatar.resize((110, 110))

    mask = Image.new("L", (110, 110), 0)
    ImageDraw.Draw(mask).ellipse((0, 0, 110, 110), fill=255)
    avatar.putalpha(mask)

    spacing = 60
    typing_speed = 6

    cycle_lengths = [len(seq) * typing_speed for seq in sequences]
    total_cycle = sum(cycle_lengths)
   
    total_frames = total_cycle + 30

    for frame in range(total_frames):
        img = base_bg.copy()
        draw = ImageDraw.Draw(img)

        # XO pattern
        pattern_layer = Image.new("RGBA", (width * 2, height), (0, 0, 0, 0))
        p_draw = ImageDraw.Draw(pattern_layer)

        for y in range(0, height, spacing):
            for x in range(0, width * 2, spacing):
                p_draw.text((x, y), "X", font=font_small, fill=(255, 255, 255, 50))
                p_draw.text((x + 25, y + 25), "O", font=font_small, fill=(255, 255, 255, 50))

        offset = (frame * 4) % spacing
        cropped_pattern = pattern_layer.crop((offset, 0, offset + width, height))
        img = Image.alpha_composite(img, cropped_pattern)

        draw = ImageDraw.Draw(img)

        # Language typing logic
        cycle_frame = frame % total_cycle
        cumulative = 0

        for seq, seq_length in zip(sequences, cycle_lengths):
            if cycle_frame < cumulative + seq_length:
                local_frame = cycle_frame - cumulative
                letter_index = min(len(seq)-1, local_frame // typing_speed)
                welcome_text = seq[letter_index]
                break
            cumulative += seq_length

        draw.text((60, 60), welcome_text, font=font_title, fill=(255, 255, 255))

        # User info
        draw.text((200, 150), username, font=font_user, fill=(255, 255, 255))
        draw.text((200, 200), member_count, font=font_small, fill=(230, 230, 255))
        draw.text((200, 230), join_time, font=font_small, fill=(230, 230, 255))

        img.paste(avatar, (60, 150), avatar)

        # Stripes
        stripe_canvas = Image.new("RGBA", (width * 2, height), (0, 0, 0, 0))
        s_draw = ImageDraw.Draw(stripe_canvas)

        stripe_y = height - 80
        stripe_height = 60
        stripe_spacing = 180
        stripe_width = 90

        for i in range(0, width * 2, stripe_spacing):
            x = i
            s_draw.polygon([
                (x, stripe_y),
                (x + stripe_width, stripe_y),
                (x + stripe_width - 35, stripe_y + stripe_height),
                (x - 35, stripe_y + stripe_height)
            ], fill=(255, 255, 255, 245))

        stripe_offset = (frame * 6) % stripe_spacing
        cropped_stripes = stripe_canvas.crop(
            (stripe_spacing - stripe_offset, 0,
             stripe_spacing - stripe_offset + width, height)
        )

        img = Image.alpha_composite(img, cropped_stripes)
        draw = ImageDraw.Draw(img)

        # ---- STAGGERED AS STYLE ----
        letter_spacing = -8   # reduced so S moves closer (left)

        a_width = draw.textlength("A", font=font_logo)
        s_width = draw.textlength("S", font=font_logo)

        as_total_width = a_width + s_width + letter_spacing

        as_x = width - as_total_width - 140
        as_y = 40

        # Glow
        for glow in [45, 30, 15]:
            glow_layer = Image.new("RGBA", img.size, (0, 0, 0, 0))
            glow_draw = ImageDraw.Draw(glow_layer)

            glow_draw.text((as_x, as_y - 12), "A",
                           font=font_logo,
                           fill=(255, 255, 255, 220))

            glow_draw.text((as_x + a_width + letter_spacing, as_y),
                           "S",
                           font=font_logo,
                           fill=(255, 255, 255, 220))

            glow_layer = glow_layer.filter(ImageFilter.GaussianBlur(glow))
            img = Image.alpha_composite(img, glow_layer)

        draw = ImageDraw.Draw(img)

        draw.text((as_x, as_y - 12), "A", font=font_logo, fill=(255, 255, 255))
        draw.text((as_x + a_width + letter_spacing, as_y),
                  "S", font=font_logo, fill=(255, 255, 255))

        # Link
        draw.text((as_x - 100, as_y + 115),
                  "https://discord.gg/arabsstudio",
                  font=font_link,
                  fill=(255, 255, 255, 160))

        frames.append(img)

    gif_path = f"welcome_{member.id}.gif"

    frames[0].save(
        gif_path,
        save_all=True,
        append_images=frames[1:],
        duration=60,
        loop=0,
        disposal=2,
        optimize=True
    )

    return gif_path


@bot.event
async def on_member_join(member):
    channel = bot.get_channel(WELCOME_CHANNEL_ID)

    gif = await create_welcome_gif(member)

    await channel.send(
        content=f"{member.mention}, Welcome to Arab’s Studio — we’re glad to have you here!",
        file=discord.File(gif)
    )

@bot.command()
async def testwelcome(ctx):
    gif = await create_welcome_gif(ctx.author)

    await ctx.send(
        content=f"{ctx.author.mention}, Welcome to Arab’s Studio — we’re glad to have you here!",
        file=discord.File(gif)
    )

bot.run(TOKEN)